name: example
publish_to: "none"
version: 0.0.1

environment:
  sdk: ">=3.0.5 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  custom_lint: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lines_longer_than:
    path: ../
